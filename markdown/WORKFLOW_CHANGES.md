# Workflow Changes

| Change ID | File | Description | Reason |
|-----------|------|-------------|--------|
| 1 | `workflows/virtual_dressing_room.json` | Added sticky‑note cluster describing supported image models, video models, quantization & VRAM guidance, and download links. | Provide quick reference for users. |
| 2 | `workflows/virtual_dressing_room.json` | Minor formatting cleanup of note node properties (removed stray whitespace). | Improve readability. |

The full diff will be attached as a separate patch file (see `workflow_changes.patch`).